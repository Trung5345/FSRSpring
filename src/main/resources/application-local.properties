spring.datasource.url=jdbc:mysql://localhost:3307/fsrspring?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8&useUnicode=true
spring.datasource.username=fsr_user
spring.datasource.password=fsr_pass

app.frontend.base-url=http://localhost:3000
app.backend.base-url=http://localhost:8080

# Redis: trỏ đến Docker Redis mapped ra localhost:6380 (tránh REDISHOST=redis từ .env leak vào shell)
spring.data.redis.host=localhost
spring.data.redis.port=6380
spring.data.redis.password=${REDISPASSWORD:fsr_redis_pass_123}
spring.session.store-type=redis

# JWT for local login (dev only — not for production)
app.jwt.secret=local-dev-secret-key-minimum-32-bytes-long!

spring.sql.init.mode=never
